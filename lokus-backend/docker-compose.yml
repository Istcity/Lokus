version: "3.9"
services:
  postgis:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: lokus
      POSTGRES_PASSWORD: lokus
      POSTGRES_DB: lokus_geo
    ports:
      - "5432:5432"
    volumes:
      - lokus_pg:/var/lib/postgresql/data

volumes:
  lokus_pg:
